<% if user_signed_in? %>
	<h1>Welcome</h1>
		
<% else %>
	<div class="hero-unit">
		<h1>Welcome to Keep In Contact!</h1>
		<p>
			Use Keep In Contact to allow people who know you access to each other.

		</p>
		<p>
			<%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
		</p>
	</div>
<% end %>